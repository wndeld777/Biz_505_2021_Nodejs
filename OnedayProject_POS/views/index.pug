extends layout

block content
	link(rel="stylesheet" href="/css/index.css")
	each table,index in TABLES
		if(table.order_count>0)
			div.table.order_yes(data-table_id=table.id) !{table.table_name}
				span ( !{table.order_count} )
		else 
			div.table(data-table_id=table_id) !{table.table_name}
	script.
		const main_section = document.querySelector("section.main")
		if(main_section){
			main_section.addEventListener("click",(e)=>{
				const target = e.target;

				if(target.tagName === "DIV" && target.className.includes("table")){
					const table_id = target.dataset.table;
					document.location.href = `/pos/order/${table}`;
				}
			})
		}